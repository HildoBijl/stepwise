// The PositionedVector class represents two points in space: a start and an end.

const { PositionedVector } = require('../CAS/linearAlgebra')

module.exports.SOtoFO = obj => new PositionedVector(obj)