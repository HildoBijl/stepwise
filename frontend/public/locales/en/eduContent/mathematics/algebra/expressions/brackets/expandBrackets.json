{
  "theory": {
    "intro": "<par>We know how to calculate a multiplication like <inline-math/>. But what do we do with <inline-math/>? The answer is: we can rewrite this by expanding brackets.</par>",
    "rule": "<head>The rule</head><par>If we have a number multiplication, like for instance <inline-math/>, then we can visualize this multiplication using blocks.</par><block-drawing/><par>From this, we see that <inline-math/> can be rewritten to <inline-math/>.</par><par>This works exactly the same if we have an expression like <inline-math/>. Here, <inline-math/>, <inline-math/> and <inline-math/> can be any type of factor, like for instance <inline-math/> or anything similar. We can again visualize this.</par><block-drawing/><par>From this visualization, we see that we may rewrite <inline-math/> to <inline-math/>. We call this <term>expanding the brackets</term>. The accompanying rule is <block-math/> Note that we multiply the factor outside of the brackets (here <inline-math/>) separately, with each of the terms inside the brackets (here <inline-math/> and <inline-math/>).</par>",
    "steps": "<head>The steps</head><par>To expand brackets, like for instance with <inline-math/>, we take the following steps.</par><list><item><a>Multiply the factor outside of the brackets separately with each term inside the brackets. So <inline-math/> becomes <inline-math/>.</a></item><item><b>For each of the resulting terms, simplify any potential number multiplications. So <inline-math/> becomes <inline-math/> and <inline-math/> becomes <inline-math/>.</b></item><item><c>Also merge any repeated multiplications into powers. So <inline-math/> becomes <inline-math/>. The final result <inline-math/> is as easy as you can possibly write it.</c></item></list>"
  },
  "meta": {
    "learningGoals": "<par>In this skill you should master <term>expanding brackets</term>. If you see a term like <inline-math/> you should turn this into <inline-math/>. This includes simplifying the number multiplications (so <inline-math/> becomes <inline-math/>) and turning multiplications of identical factors into powers (so <inline-math/> becomes <inline-math/>).</par>"
  }
}