import React from 'react'

import { Translation } from 'i18n'
import { Par, Term } from 'ui/components'

export function Meta() {
	return <Translation entry="learningGoals">
		<Par>The goal of this skill is to get familiar with <Term>entering physical quantities</Term> that have both a number and a unit. On top of this, it introduces the attachments where students can look up relevant data.</Par>
	</Translation>
}
