import React from 'react'

import { Translation } from 'i18n'
import { Par, Term } from 'ui/components'

export function Meta() {
	return <Translation entry="learningGoals">
		<Par>The goal of this skill is to get familiar with <Term>entering decimal numbers</Term>. On top of this, students get to experience the useful features built into <Term>Step-Wise input fields</Term>.</Par>
	</Translation>
}
