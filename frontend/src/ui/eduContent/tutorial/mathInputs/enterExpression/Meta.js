import React from 'react'

import { Translation } from 'i18n'
import { Par, Term } from 'ui/components'

export function Meta() {
	return <Translation>
		<Par>In this skill students get familiar with entering a variety of <Term>mathematical expressions</Term>, not only with fundamentals (fractions, powers, subscripts) but also with more advanced functions (sines/cosines, roots,  logarithms). The focus lies on the advanced tricks of entering these expressions, either on a smartphone or on a regular computer keyboard.</Par>
	</Translation>
}
