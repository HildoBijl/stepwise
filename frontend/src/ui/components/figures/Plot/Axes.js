

export const Axes = forwardRef(({ plotSettings, ...options }, ref) => {
	// ToDo: generate the Axes using the props, applying shapes where needed.
	// Settings include: { useTicks, useGridLines, numberToTick, label, useSvgTicks, useSvgLabels }.

})
export default Axes